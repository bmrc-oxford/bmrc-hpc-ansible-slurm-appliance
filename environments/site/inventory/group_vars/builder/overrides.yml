proxy_basic_user: ff28d9
proxy_basic_password: "{{ vault_proxy_basic_password }}"
proxy_http_address: nvsmirror00-ext.nvs.bmrc.ox.ac.uk

# no MIG, so can do this for build speed:
slurm_recompile_with_nvml: false
