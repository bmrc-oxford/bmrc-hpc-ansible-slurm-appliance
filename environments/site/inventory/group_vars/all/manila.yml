os_manila_mount_shares:
  - share_name: ms-home
    mount_path: /home
  - share_name: ms-apps
    mount_path: /apps
  # For /data ONLY, use a separate share for each environment
  - share_name: "{{ openhpc_cluster_name }}-data"
    mount_path: /data
