os_manila_mount_shares:
  - share_name: "{{ openhpc_cluster_name }}-home"
    mount_path: /home
  # For /apps ONLY, use the same share in all environments
  - share_name: ms-apps
    mount_path: /apps
  - share_name: "{{ openhpc_cluster_name }}-data"
    mount_path: /data
