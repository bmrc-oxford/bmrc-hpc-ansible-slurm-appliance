[nvs_freeipa_server]
ipa-a.nvs.bmrc.ox.ac.uk ansible_host=ipa-a.nvs.bmrc.ox.ac.uk ansible_user=centos

[freeipa:children]
# As for environments/common/inventory/groups
# Allows defining variables common to freeipa_server and _client
freeipa_client
nvs_freeipa_server

[freeipa_server:children]
nvs_freeipa_server
